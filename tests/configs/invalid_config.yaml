model:
  name: invalid_model
  params:
    input_dim: 128
    output_dim: 64
    hidden_dim: 32
    
optimizer:
  name: adam
  params:
    learning_rate: 1e-3

loss:
  name: ssiKLdiv
  params: {}

training:
  epochs: 2
  batch_size: 8